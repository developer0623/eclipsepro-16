import { HideCompletePipe } from './hide-complete.pipe';

describe('HideCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new HideCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
