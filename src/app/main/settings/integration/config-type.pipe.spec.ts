import { ConfigTypePipe } from './config-type.pipe';

describe('ConfigTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ConfigTypePipe();
    expect(pipe).toBeTruthy();
  });
});
