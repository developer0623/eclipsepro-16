import { IfClaimDirective } from './if-claim.directive';

describe('IfClaimDirective', () => {
  it('should create an instance', () => {
    const directive = new IfClaimDirective();
    expect(directive).toBeTruthy();
  });
});
