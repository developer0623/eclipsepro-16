import { BtfMarkdownDirective } from './btf-markdown.directive';

describe('BtfMarkdownDirective', () => {
  it('should create an instance', () => {
    const directive = new BtfMarkdownDirective();
    expect(directive).toBeTruthy();
  });
});
