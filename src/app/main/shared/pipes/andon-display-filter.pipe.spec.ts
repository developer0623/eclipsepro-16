import { AndonDisplayFilterPipe } from './andon-display-filter.pipe';

describe('AndonDisplayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AndonDisplayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
