import { SummaryPercentPipe } from './summary-percent.pipe';

describe('SummaryPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
