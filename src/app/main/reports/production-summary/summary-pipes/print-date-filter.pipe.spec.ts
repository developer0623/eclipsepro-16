import { PrintDateFilterPipe } from './print-date-filter.pipe';

describe('PrintDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PrintDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
