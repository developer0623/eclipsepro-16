import { TimebarPercentPipe } from './timebar-percent.pipe';

describe('TimebarPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new TimebarPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
